  // cy.get('[routerlink="instructions"]').should('exist').click();
  // cy.url().should('include', '/panel/instructions');
  // cy.get('button[id="brandSelectDropdown"]').should('be.visible')
  // cy.contains('Porsche').click();

  // // cy.get('button#brandSelectDropdown.dropdown-toggle.brand-select-dropdown_toggle.btn.btn-secondary')
  // // .should('be.visible')
  // // .as('brandSelectDropdown')
  // // .click();
  // // cy.get('@brandSelectDropdown').filter(':contains("Porsche")').click();

  // cy.get('button[id="modelSelectDropdown"]').should('be.visible')
  // cy.contains('Panamera').click();
  // // cy.get('button[id="modelSelectDropdown"]').should('be.visible')
  // // .as('modelSelectDropdown')
  // // .click();
  // // cy.get('@modelSelectDropdown').filter(':contains("Panamera")').click();

  // cy.get('button[class="instructions-search-controls_search btn btn-primary"]')
  // .eq(0)
  // .should('be.visible')
  // .click();
  // cy.get('a[class="instruction-link"]').contains('Porsche Panamera.pdf');